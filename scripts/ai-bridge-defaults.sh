#!/bin/bash
# Canonical defaults for ai-bridge configuration.
# All components should reference this file as the single source of truth.
# Cross-language files (Lua, YAML) that cannot source this file should
# add a comment pointing here.
AI_BRIDGE_DIR="${AI_BRIDGE_DIR:-${HOME}/.ai-bridge}"
