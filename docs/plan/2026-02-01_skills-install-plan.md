# Plan

このリポジトリの `.codex/skills` を `${HOME}/.codex/skills/` に安全にコピーする make ターゲットを追加する。上書き確認は全体で1回にまとめ、英語プロンプトでデフォルトは「上書きしない」にする。

## Scope

- In: `Makefile` に安全なコピー用ターゲット追加、上書き確認の対話フロー、必要ならヘルプ文更新
- Out: スキルの中身変更、他リポジトリへの配布機構の整備

## Action items

[ ] `Makefile` の既存ターゲットとパターンを確認する
[ ] `.codex/skills` と `${HOME}/.codex/skills/` の想定パス・挙動を整理する
[ ] 上書き確認を全体1回で行う仕様に固める（英語プロンプト、デフォルトNo）
[ ] 既存スキル検出→確認→コピーの実装方針を `Makefile` に落とす
[ ] エッジケース（空ディレクトリ、スキル未存在、スペース含むパス）を考慮する
[ ] 動作確認用の手順（ダミースキル作成→上書き確認→結果確認）を用意する
[ ] 必要なら README に使い方を追記する

## Open questions

- なし
