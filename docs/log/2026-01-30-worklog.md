# 2026-01-30 Work Log

## Summary

- Reorganized repository layout into `environment/` and `nvim/` directories.
- Updated Docker build context, CI workflows, Dependabot paths, and documentation to match new paths.
- Added and refined AGENTS rules (root, environment, nvim) and lint/format rules.
- Added markdownlint config and ran markdownlint fix.

## Details

### Repository structure changes

- Moved Docker artifacts to `environment/docker/`.
- Moved toolchain definitions to `environment/tools/{go,python,node}/`.
- Moved Neovim config to `nvim/config/`.
- Updated `.dockerignore`, `README.md`, and GitHub Actions to use new paths.

### AGENTS rules

- Root `AGENTS.md` updated to reflect new structure and lint/format rules.
- Added `environment/AGENTS.md` with environment-specific guidance.
- Added `nvim/AGENTS.md` with Neovim-specific guidance and Lua formatting rule (`stylua .`).

### Linting

- Added `.markdownlint-cli2.yaml`.
- Ran `markdownlint-cli2 --fix *` with no errors.

## Commands used

- `docker compose -f environment/docker/docker-compose.yml build`
- `docker compose -f environment/docker/docker-compose.yml up -d`
- `markdownlint-cli2 --fix *`

## PRs created

- chore-restructure-layout (merged)
- chore-add-agents-rules (draft PR: #310)
